/* COMPILER

"tsc.file -w" to run watch mode, so the file compiles automaticzlly.

"tsc --init" to add all ts files as a ts project.  Creates tsconfig.json. After we can simply run "tsc" command to compile. If we run
watch mode, it will run it for all ts files.
*/